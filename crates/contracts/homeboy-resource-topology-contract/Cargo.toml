[package]
name = "homeboy-resource-topology-contract"
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["Chris Huber <chubes@extrachill.com>"]
description = "Versioned read-only Homeboy resource topology contracts"
repository = "https://github.com/Extra-Chill/homeboy"
publish = false

[lib]
name = "homeboy_resource_topology_contract"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
