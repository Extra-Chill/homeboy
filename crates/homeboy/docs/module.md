# module

Execute CLI-compatible modules (automation scripts).

```bash
homeboy module list
homeboy module run <module-id> [args...]
homeboy module setup <module-id>
```

Use `homeboy help module` for the authoritative flag list.
