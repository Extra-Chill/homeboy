# pin

Pin frequently used files and logs.

```bash
homeboy pin list <project-id>
homeboy pin add <project-id> <path> --type <type>
homeboy pin remove <project-id> <path>
```

Use `homeboy help pin` for the authoritative flag list.
