# projects

List configured projects.

```bash
homeboy projects
homeboy projects --current
homeboy projects --json
```

- `--current`: prints only the active project ID
- `--json`: prints a JSON response envelope
